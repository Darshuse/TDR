
package com.eastnets.textbreak.bean;

import org.springframework.context.annotation.Configuration;

@Configuration
public class PropertyPlaceholderBean {

	/*
	 * @Bean public PropertyPlaceholderConfigurer placeholderConfigurer() {
	 * 
	 * String inPropertiesFile = "textBreakConfig.properties"; InputStream is = null; try { is = new FileInputStream(inPropertiesFile); } catch (FileNotFoundException e) { e.printStackTrace(); }
	 * Properties props = new Properties(); try { props.load(is); } catch (IOException e) { // TODO Auto-generated catch block e.printStackTrace(); } PropertyPlaceholderConfigurer configurer = new
	 * PropertyPlaceholderConfigurer(); configurer.setIgnoreUnresolvablePlaceholders(true); configurer.setOrder(1); configurer.setProperties(props); return configurer; }
	 */

}
