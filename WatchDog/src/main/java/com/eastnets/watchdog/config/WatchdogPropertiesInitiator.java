package com.eastnets.watchdog.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WatchdogPropertiesInitiator {
	/*
	 * private static final Logger LOGGER = LogManager.getLogger(WatchdogPropertiesInitiator.class);
	 * 
	 * public WatchdogPropertiesInitiator() { }
	 * 
	 * @Bean public static PropertyPlaceholderConfigurer placeholderConfigurer() {
	 * 
	 * Properties props = new Properties();
	 * 
	 * try {
	 * 
	 * props.load(new ClassPathResource("wdserver.properties").getInputStream());
	 * 
	 * } catch (IOException e) { LOGGER.error("context", e); }
	 * 
	 * PropertyPlaceholderConfigurer configurer = new PropertyPlaceholderConfigurer(); configurer.setIgnoreUnresolvablePlaceholders(true); configurer.setOrder(1); configurer.setProperties(props);
	 * 
	 * return configurer; }
	 */

}
